# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Added: Playwright E2E test artifacts (TMAIL-36)
test-results/
playwright-report/
# Note: e2e/screenshots/*.png are intentionally tracked so the curated baseline
# can be reviewed in PRs. Drop new shots into git locally if you want them
# part of the baseline; they are also rebuilt every test run.
